using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum RoleType
{
    None = 0,
    Player = 1,
    Waiter = 2,
    Customer = 3,
    InteractiveNpc = 4
}

public enum RoleHealth
{
    Good = 0,
    Normal=1,
    Bad = 2
}
