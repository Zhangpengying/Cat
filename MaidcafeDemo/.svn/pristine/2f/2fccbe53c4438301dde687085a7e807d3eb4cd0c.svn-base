using System.Collections;
using System.Collections.Generic;
using System.IO;
using System.Text.RegularExpressions;
using UnityEngine;

public class DIYBagIO : Singleton<DIYBagIO>
{
    public void Write()
    {
        //StreamWriter sw = new StreamWriter(StaticVar.SavePath + "/DIYBag.txt");
        //foreach (var item in MessageSend.instance.CurrentHavePropertys)
        //{
        //    sw.Write("ID" + item.ID);
           
        //}
        //sw.Close();
    }

    public void Read()
    {
        //MessageSend.instance.CurrentHavePropertys.Clear();
        //StreamReader sr = new StreamReader(StaticVar.SavePath + "/DIYBag.txt");

        //string B = sr.ReadToEnd();
        //if (B != null)
        //{
        //    string[] C = Regex.Split(B, "ID");
        //    for (int i = 0; i < C.Length; i++)
        //    {
        //        if (C[i] != "")
        //        {
        //            int ID = int.Parse(C[i]);
        //            Property property = MessageSend.instance.propertyCfgs[ID];
        //            MessageSend.instance.CurrentHavePropertys.Add(property);
        //        }
        //    }
        //}
        //sr.Close();
    }
}
